#include "main.h"

/**
 * print_triangle - prints a tirangle
 * @size: number of lines
 * Return: no return
 */

void print_triangle(int size)
{
	int i, j, k

	for (i = 0; i < n; i++)
	{

		k = (n - i - 1);
		for (j = 0; j < n; j++)
		{
			if (j < k)










}
